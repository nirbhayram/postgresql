import { SectionMarkup } from "./section-markup";

const html = "<div class=\"section-hero\">\n        <div class=\"section-num-big\">09</div>\n        <div class=\"section-tag\">// transactions</div>\n        <div class=\"section-title\">Transactions & AutoCommit</div>\n        <p class=\"section-desc\">PostgreSQL uses BEGIN/COMMIT/ROLLBACK just like MySQL. It adds SAVEPOINTs and four isolation levels backed by its powerful MVCC engine.</p>\n      </div>\n\n      <h3 class=\"block-title\">Basic Transaction Flow</h3>\n      <div class=\"code-block\">\n        <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n        <pre class=\"code\"><span class=\"cm\">-- Start a transaction</span>\n<span class=\"kw\">BEGIN</span>;\n\n<span class=\"kw\">UPDATE</span> users <span class=\"kw\">SET</span> salary = <span class=\"num\">80000</span> <span class=\"kw\">WHERE</span> id = <span class=\"num\">5</span>;\n<span class=\"kw\">UPDATE</span> users <span class=\"kw\">SET</span> salary = <span class=\"num\">75000</span> <span class=\"kw\">WHERE</span> id = <span class=\"num\">3</span>;\n\n<span class=\"kw\">COMMIT</span>;    <span class=\"cm\">-- save all changes</span>\n<span class=\"cm\">-- or</span>\n<span class=\"kw\">ROLLBACK</span>; <span class=\"cm\">-- undo everything since BEGIN</span></pre>\n      </div>\n\n      <div class=\"info-warn info-box\"><span class=\"icon\">⚠️</span><span>Unlike MySQL's <code>SET autocommit = 0</code>, PostgreSQL has no session-level autocommit toggle in SQL. Use <code>BEGIN</code> explicitly to start a transaction block.</span></div>\n\n      <div class=\"adv-box\">\n        <div class=\"adv-header\"><span class=\"adv-icon\">⚡</span><span class=\"adv-label\">Advanced — SAVEPOINT (partial rollback)</span><span class=\"adv-badge\">POWER FEATURE</span></div>\n        <div class=\"adv-body\">\n          <p class=\"prose\">SAVEPOINTs let you roll back to a named point inside a transaction without losing the entire transaction.</p>\n          <div class=\"code-block\">\n            <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n            <pre class=\"code\"><span class=\"kw\">BEGIN</span>;\n\n<span class=\"kw\">UPDATE</span> users <span class=\"kw\">SET</span> salary = <span class=\"num\">90000</span> <span class=\"kw\">WHERE</span> id = <span class=\"num\">1</span>;\n<span class=\"kw\">SAVEPOINT</span> after_first;\n\n<span class=\"kw\">UPDATE</span> users <span class=\"kw\">SET</span> salary = <span class=\"num\">999999</span> <span class=\"kw\">WHERE</span> id = <span class=\"num\">2</span>;  <span class=\"cm\">-- mistake</span>\n\n<span class=\"kw\">ROLLBACK TO</span> after_first;   <span class=\"cm\">-- undo only the 2nd update</span>\n<span class=\"kw\">RELEASE SAVEPOINT</span> after_first;\n\n<span class=\"kw\">COMMIT</span>;  <span class=\"cm\">-- first update is committed</span></pre>\n          </div>\n        </div>\n      </div>\n\n      <div class=\"adv-box\">\n        <div class=\"adv-header\"><span class=\"adv-icon\">⚡</span><span class=\"adv-label\">Advanced — Isolation Levels & MVCC</span><span class=\"adv-badge\">INTERVIEW</span></div>\n        <div class=\"adv-body\">\n          <table class=\"data-table\">\n            <tr><th>Level</th><th>Dirty Read</th><th>Non-Repeatable</th><th>Phantom Read</th></tr>\n            <tr><td><code>READ UNCOMMITTED</code></td><td>Possible</td><td>Possible</td><td>Possible</td></tr>\n            <tr><td><code>READ COMMITTED</code> (default)</td><td>No</td><td>Possible</td><td>Possible</td></tr>\n            <tr><td><code>REPEATABLE READ</code></td><td>No</td><td>No</td><td>Possible</td></tr>\n            <tr><td><code>SERIALIZABLE</code></td><td>No</td><td>No</td><td>No</td></tr>\n          </table>\n          <div class=\"code-block\">\n            <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n            <pre class=\"code\"><span class=\"kw\">BEGIN</span>;\n<span class=\"kw\">SET TRANSACTION ISOLATION LEVEL</span> REPEATABLE READ;\n<span class=\"cm\">-- ... queries ...</span>\n<span class=\"kw\">COMMIT</span>;</pre>\n          </div>\n          <p class=\"prose\">PostgreSQL's MVCC (Multi-Version Concurrency Control) keeps old row versions so readers never block writers and writers never block readers — a major performance advantage.</p>\n        </div>\n      </div>";

export default function Section8() {
  return <SectionMarkup html={html} />;
}
