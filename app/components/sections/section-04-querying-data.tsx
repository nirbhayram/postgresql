import { SectionMarkup } from "./section-markup";

const html = "<div class=\"section-hero\">\n        <div class=\"section-num-big\">05</div>\n        <div class=\"section-tag\">// querying</div>\n        <div class=\"section-title\">Querying Data — SELECT</div>\n        <p class=\"section-desc\">Basic SELECT works identically to MySQL. PostgreSQL adds ILIKE, regex operators, and the powerful window functions that are extremely common in interviews.</p>\n      </div>\n\n      <h3 class=\"block-title\">Standard Filtering — same as MySQL</h3>\n      <div class=\"code-block\">\n        <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n        <pre class=\"code\"><span class=\"kw\">SELECT</span> * <span class=\"kw\">FROM</span> users <span class=\"kw\">WHERE</span> gender = <span class=\"str\">'Male'</span>;\n<span class=\"kw\">SELECT</span> * <span class=\"kw\">FROM</span> users <span class=\"kw\">WHERE</span> id <span class=\"kw\">BETWEEN</span> <span class=\"num\">5</span> <span class=\"kw\">AND</span> <span class=\"num\">20</span>;\n<span class=\"kw\">SELECT</span> * <span class=\"kw\">FROM</span> users <span class=\"kw\">WHERE</span> gender <span class=\"kw\">IN</span> (<span class=\"str\">'Male'</span>, <span class=\"str\">'Other'</span>);\n<span class=\"kw\">SELECT</span> * <span class=\"kw\">FROM</span> users <span class=\"kw\">WHERE</span> name <span class=\"kw\">LIKE</span> <span class=\"str\">'A%'</span>;\n<span class=\"kw\">SELECT</span> * <span class=\"kw\">FROM</span> users <span class=\"kw\">WHERE</span> date_of_birth <span class=\"kw\">IS NULL</span>;\n<span class=\"kw\">SELECT</span> * <span class=\"kw\">FROM</span> users <span class=\"kw\">ORDER BY</span> name <span class=\"kw\">DESC LIMIT</span> <span class=\"num\">5</span> <span class=\"kw\">OFFSET</span> <span class=\"num\">10</span>;</pre>\n      </div>\n\n      <div class=\"info-warn info-box\"><span class=\"icon\">⚠️</span><span>PostgreSQL does not support the <code>LIMIT offset, count</code> MySQL shorthand. Always use the explicit <code>LIMIT n OFFSET m</code> syntax.</span></div>\n\n      <h3 class=\"block-title\">PostgreSQL-Specific Filtering</h3>\n      <div class=\"code-block\">\n        <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n        <pre class=\"code\"><span class=\"cm\">-- ILIKE = case-insensitive LIKE (PostgreSQL only)</span>\n<span class=\"kw\">SELECT</span> * <span class=\"kw\">FROM</span> users <span class=\"kw\">WHERE</span> name <span class=\"kw\">ILIKE</span> <span class=\"str\">'alice%'</span>;\n\n<span class=\"cm\">-- SIMILAR TO — regex-like pattern</span>\n<span class=\"kw\">SELECT</span> * <span class=\"kw\">FROM</span> users <span class=\"kw\">WHERE</span> name <span class=\"kw\">SIMILAR TO</span> <span class=\"str\">'(Alice|Bob)%'</span>;\n\n<span class=\"cm\">-- ~ operator = POSIX regex (case-sensitive)</span>\n<span class=\"kw\">SELECT</span> * <span class=\"kw\">FROM</span> users <span class=\"kw\">WHERE</span> email ~ <span class=\"str\">'^[a-z]+@gmail\\.com$'</span>;\n\n<span class=\"cm\">-- ~* operator = case-insensitive regex</span>\n<span class=\"kw\">SELECT</span> * <span class=\"kw\">FROM</span> users <span class=\"kw\">WHERE</span> name ~* <span class=\"str\">'alice'</span>;\n\n<span class=\"cm\">-- SQL standard FETCH (works cross-platform)</span>\n<span class=\"kw\">SELECT</span> * <span class=\"kw\">FROM</span> users <span class=\"kw\">ORDER BY</span> salary <span class=\"kw\">DESC</span>\n<span class=\"kw\">FETCH FIRST</span> <span class=\"num\">5</span> <span class=\"kw\">ROWS ONLY</span>;</pre>\n      </div>\n\n      <div class=\"adv-box\">\n        <div class=\"adv-header\"><span class=\"adv-icon\">⚡</span><span class=\"adv-label\">Advanced — Window Functions (ROW_NUMBER, RANK, LAG, LEAD)</span><span class=\"adv-badge\">HIGH PRIORITY</span></div>\n        <div class=\"adv-body\">\n          <p class=\"prose\">Window functions perform calculations across a set of rows related to the current row, without collapsing rows like GROUP BY. Extremely common in analytics and data engineering interviews.</p>\n          <div class=\"code-block\">\n            <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n            <pre class=\"code\"><span class=\"kw\">SELECT</span> name, gender, salary,\n  <span class=\"cm\">-- Rank within each gender group</span>\n  <span class=\"fn\">RANK</span>() <span class=\"kw\">OVER</span> (<span class=\"kw\">PARTITION BY</span> gender <span class=\"kw\">ORDER BY</span> salary <span class=\"kw\">DESC</span>) <span class=\"kw\">AS</span> rank_in_gender,\n  <span class=\"cm\">-- Overall ranking by salary</span>\n  <span class=\"fn\">DENSE_RANK</span>() <span class=\"kw\">OVER</span> (<span class=\"kw\">ORDER BY</span> salary <span class=\"kw\">DESC</span>) <span class=\"kw\">AS</span> dense_rank,\n  <span class=\"cm\">-- Previous row's salary</span>\n  <span class=\"fn\">LAG</span>(salary, <span class=\"num\">1</span>) <span class=\"kw\">OVER</span> (<span class=\"kw\">ORDER BY</span> id) <span class=\"kw\">AS</span> prev_salary,\n  <span class=\"cm\">-- Running total of salary</span>\n  <span class=\"fn\">SUM</span>(salary) <span class=\"kw\">OVER</span> (<span class=\"kw\">ORDER BY</span> id) <span class=\"kw\">AS</span> running_total\n<span class=\"kw\">FROM</span> users;</pre>\n          </div>\n          <table class=\"data-table\">\n            <tr><th>Function</th><th>Description</th></tr>\n            <tr><td><code>ROW_NUMBER()</code></td><td>Sequential number, no ties</td></tr>\n            <tr><td><code>RANK()</code></td><td>Rank with gaps on ties (1, 1, 3)</td></tr>\n            <tr><td><code>DENSE_RANK()</code></td><td>Rank without gaps (1, 1, 2)</td></tr>\n            <tr><td><code>LAG(col, n)</code></td><td>Value from n rows before</td></tr>\n            <tr><td><code>LEAD(col, n)</code></td><td>Value from n rows ahead</td></tr>\n            <tr><td><code>NTILE(n)</code></td><td>Divide into n equal buckets</td></tr>\n            <tr><td><code>SUM() OVER()</code></td><td>Running / cumulative total</td></tr>\n          </table>\n        </div>\n      </div>";

export default function Section4() {
  return <SectionMarkup html={html} />;
}
