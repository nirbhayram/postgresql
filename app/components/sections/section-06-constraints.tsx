import { SectionMarkup } from "./section-markup";

const html = "<div class=\"section-hero\">\n        <div class=\"section-num-big\">07</div>\n        <div class=\"section-tag\">// data integrity</div>\n        <div class=\"section-title\">Constraints</div>\n        <p class=\"section-desc\">PostgreSQL supports all MySQL constraints plus two powerful extras: Exclusion constraints for overlap prevention, and Deferrable constraints for end-of-transaction checking.</p>\n      </div>\n\n      <h3 class=\"block-title\">All constraints in one table</h3>\n      <div class=\"code-block\">\n        <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n        <pre class=\"code\"><span class=\"kw\">CREATE</span> <span class=\"kw\">TABLE</span> users (\n  id         <span class=\"fn\">INT</span> <span class=\"kw\">GENERATED ALWAYS AS IDENTITY PRIMARY KEY</span>,\n  name       <span class=\"fn\">VARCHAR</span>(<span class=\"num\">100</span>) <span class=\"kw\">NOT NULL</span>,\n  email      <span class=\"fn\">VARCHAR</span>(<span class=\"num\">100</span>) <span class=\"kw\">UNIQUE NOT NULL</span>,\n  gender     <span class=\"fn\">VARCHAR</span>(<span class=\"num\">10</span>) <span class=\"kw\">CHECK</span> (gender <span class=\"kw\">IN</span> (<span class=\"str\">'Male'</span>, <span class=\"str\">'Female'</span>, <span class=\"str\">'Other'</span>)),\n  salary     <span class=\"fn\">INT</span> <span class=\"kw\">CHECK</span> (salary > <span class=\"num\">0</span>),\n  is_active  <span class=\"fn\">BOOLEAN</span> <span class=\"kw\">DEFAULT</span> <span class=\"kw\">TRUE</span>,\n  created_at <span class=\"fn\">TIMESTAMPTZ</span> <span class=\"kw\">DEFAULT</span> <span class=\"fn\">NOW</span>()\n);\n\n<span class=\"cm\">-- Alter: set NOT NULL</span>\n<span class=\"kw\">ALTER</span> <span class=\"kw\">TABLE</span> users <span class=\"kw\">ALTER</span> <span class=\"kw\">COLUMN</span> name <span class=\"kw\">SET NOT NULL</span>;\n\n<span class=\"cm\">-- Alter: drop NOT NULL</span>\n<span class=\"kw\">ALTER</span> <span class=\"kw\">TABLE</span> users <span class=\"kw\">ALTER</span> <span class=\"kw\">COLUMN</span> name <span class=\"kw\">DROP NOT NULL</span>;\n\n<span class=\"cm\">-- Add named constraint</span>\n<span class=\"kw\">ALTER</span> <span class=\"kw\">TABLE</span> users\n  <span class=\"kw\">ADD CONSTRAINT</span> chk_salary <span class=\"kw\">CHECK</span> (salary > <span class=\"num\">0</span>);\n\n<span class=\"cm\">-- Drop named constraint</span>\n<span class=\"kw\">ALTER</span> <span class=\"kw\">TABLE</span> users <span class=\"kw\">DROP CONSTRAINT</span> chk_salary;</pre>\n      </div>\n\n      <div class=\"adv-box\">\n        <div class=\"adv-header\"><span class=\"adv-icon\">⚡</span><span class=\"adv-label\">Advanced — EXCLUSION Constraints (PostgreSQL-only)</span><span class=\"adv-badge\">UNIQUE FEATURE</span></div>\n        <div class=\"adv-body\">\n          <p class=\"prose\">Exclusion constraints generalise UNIQUE — instead of checking equality, they can check any operator. The classic use-case: prevent overlapping date ranges in booking systems.</p>\n          <div class=\"code-block\">\n            <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n            <pre class=\"code\"><span class=\"kw\">CREATE EXTENSION IF NOT EXISTS</span> btree_gist;\n\n<span class=\"kw\">CREATE</span> <span class=\"kw\">TABLE</span> room_bookings (\n  room_id <span class=\"fn\">INT</span>,\n  during  <span class=\"fn\">TSRANGE</span>,  <span class=\"cm\">-- timestamp range type</span>\n  <span class=\"kw\">EXCLUDE USING</span> gist (room_id <span class=\"kw\">WITH</span> =, during <span class=\"kw\">WITH</span> &&)\n);\n<span class=\"cm\">-- The && operator checks for overlap.\n-- This prevents two bookings for the same room from overlapping.</span></pre>\n          </div>\n        </div>\n      </div>\n\n      <div class=\"adv-box\">\n        <div class=\"adv-header\"><span class=\"adv-icon\">⚡</span><span class=\"adv-label\">Advanced — Deferrable Constraints</span><span class=\"adv-badge\">PATTERN</span></div>\n        <div class=\"adv-body\">\n          <p class=\"prose\">By default, constraints are checked after every row. Deferrable constraints are checked at end-of-transaction. Useful for circular FK references or bulk inserts where intermediate states would violate constraints.</p>\n          <div class=\"code-block\">\n            <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n            <pre class=\"code\"><span class=\"kw\">ALTER</span> <span class=\"kw\">TABLE</span> users\n  <span class=\"kw\">ADD CONSTRAINT</span> fk_referred\n  <span class=\"kw\">FOREIGN KEY</span> (referred_by_id) <span class=\"kw\">REFERENCES</span> users(id)\n  <span class=\"kw\">DEFERRABLE INITIALLY DEFERRED</span>;\n<span class=\"cm\">-- Constraint is checked at COMMIT, not after each row</span></pre>\n          </div>\n        </div>\n      </div>";

export default function Section6() {
  return <SectionMarkup html={html} />;
}
