import { SectionMarkup } from "./section-markup";

const html = "<div class=\"section-hero\">\n        <div class=\"section-num-big\">02</div>\n        <div class=\"section-tag\">// foundations</div>\n        <div class=\"section-title\">Databases & Tables</div>\n        <p class=\"section-desc\">PostgreSQL introduces an extra layer — the Schema — that sits between database and tables. Understanding this three-tier hierarchy is fundamental.</p>\n      </div>\n\n      <h3 class=\"block-title\">The Three-Tier Hierarchy</h3>\n      <table class=\"data-table\">\n        <tr><th>Concept</th><th>MySQL</th><th>PostgreSQL</th></tr>\n        <tr><td>Container</td><td>Database</td><td>Database → <strong>Schema</strong> → Tables</td></tr>\n        <tr><td>Default namespace</td><td>(none)</td><td><code>public</code> schema</td></tr>\n        <tr><td>Cross-db query</td><td>Supported directly</td><td>Requires Foreign Data Wrappers</td></tr>\n        <tr><td>Connect to db</td><td><code>USE dbname;</code></td><td><code>\\c dbname</code> in psql</td></tr>\n      </table>\n\n      <h3 class=\"block-title\">Create a Database & Table</h3>\n      <div class=\"code-block\">\n        <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n        <pre class=\"code\"><span class=\"kw\">CREATE</span> <span class=\"kw\">DATABASE</span> startersql;\n\\c startersql   <span class=\"cm\">-- \\c connects to the db (replaces USE)</span>\n\n<span class=\"kw\">CREATE</span> <span class=\"kw\">TABLE</span> users (\n  id            <span class=\"fn\">INT</span> <span class=\"kw\">GENERATED ALWAYS AS IDENTITY PRIMARY KEY</span>,\n  name          <span class=\"fn\">VARCHAR</span>(<span class=\"num\">100</span>) <span class=\"kw\">NOT NULL</span>,\n  email         <span class=\"fn\">VARCHAR</span>(<span class=\"num\">100</span>) <span class=\"kw\">UNIQUE NOT NULL</span>,\n  gender        <span class=\"fn\">VARCHAR</span>(<span class=\"num\">10</span>) <span class=\"kw\">CHECK</span> (gender <span class=\"kw\">IN</span> (<span class=\"str\">'Male'</span>, <span class=\"str\">'Female'</span>, <span class=\"str\">'Other'</span>)),\n  date_of_birth <span class=\"fn\">DATE</span>,\n  created_at    <span class=\"fn\">TIMESTAMPTZ</span> <span class=\"kw\">DEFAULT</span> <span class=\"fn\">NOW</span>()  <span class=\"cm\">-- timezone-aware</span>\n);</pre>\n      </div>\n\n      <div class=\"info-warn info-box\"><span class=\"icon\">⚠️</span><span>PostgreSQL does not have MySQL's <code>ENUM</code> column modifier. Use a <code>CHECK</code> constraint (above) or create a proper enum type with <code>CREATE TYPE gender_type AS ENUM (...)</code>.</span></div>\n\n      <h3 class=\"block-title\">Data Type Comparison</h3>\n      <table class=\"data-table\">\n        <tr><th>MySQL</th><th>PostgreSQL</th><th>Notes</th></tr>\n        <tr><td><code>INT AUTO_INCREMENT</code></td><td><code>SERIAL</code> or <code>GENERATED ALWAYS AS IDENTITY</code></td><td><span class=\"badge-tbl b-blue\">IDENTITY</span> is SQL standard</td></tr>\n        <tr><td><code>TINYINT(1)</code></td><td><code>BOOLEAN</code></td><td>True boolean type</td></tr>\n        <tr><td><code>DATETIME</code></td><td><code>TIMESTAMP</code> / <code>TIMESTAMPTZ</code></td><td>Use <code>TIMESTAMPTZ</code> for timezone</td></tr>\n        <tr><td><code>JSON</code></td><td><code>JSON</code> / <code>JSONB</code></td><td><span class=\"badge-tbl b-green\">JSONB</span> is binary & indexable</td></tr>\n        <tr><td>—</td><td><code>UUID</code></td><td>Native; use <code>gen_random_uuid()</code></td></tr>\n        <tr><td>—</td><td><code>ARRAY</code></td><td>Any type: <code>tags TEXT[]</code></td></tr>\n        <tr><td>—</td><td><code>TSRANGE</code>, <code>DATERANGE</code></td><td>Range types for overlap queries</td></tr>\n      </table>\n\n      <div class=\"adv-box\">\n        <div class=\"adv-header\"><span class=\"adv-icon\">⚡</span><span class=\"adv-label\">Advanced — SERIAL vs IDENTITY (interview favourite)</span><span class=\"adv-badge\">INTERVIEW</span></div>\n        <div class=\"adv-body\">\n          <p class=\"prose\"><code>SERIAL</code> is PostgreSQL shorthand — it creates a sequence and sets the column default. The sequence is not strictly owned by the column. <code>GENERATED ALWAYS AS IDENTITY</code> is the SQL:2003 standard and is stricter — you cannot manually insert a value unless you say <code>OVERRIDING SYSTEM VALUE</code>.</p>\n          <div class=\"code-block\">\n            <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n            <pre class=\"code\"><span class=\"cm\">-- Old style (still works, not recommended)</span>\n<span class=\"kw\">CREATE</span> <span class=\"kw\">TABLE</span> users (id <span class=\"fn\">SERIAL</span> <span class=\"kw\">PRIMARY KEY</span>);\n\n<span class=\"cm\">-- Modern SQL standard (PostgreSQL 10+)</span>\n<span class=\"kw\">CREATE</span> <span class=\"kw\">TABLE</span> users (\n  id <span class=\"fn\">INT</span> <span class=\"kw\">GENERATED ALWAYS AS IDENTITY PRIMARY KEY</span>\n);\n\n<span class=\"cm\">-- Override when needed (e.g., data migration)</span>\n<span class=\"kw\">INSERT INTO</span> users (id, name) <span class=\"kw\">OVERRIDING SYSTEM VALUE</span>\n<span class=\"kw\">VALUES</span> (<span class=\"num\">100</span>, <span class=\"str\">'Alice'</span>);</pre>\n          </div>\n        </div>\n      </div>";

export default function Section1() {
  return <SectionMarkup html={html} />;
}
