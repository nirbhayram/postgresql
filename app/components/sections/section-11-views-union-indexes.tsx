import { SectionMarkup } from "./section-markup";

const html = "<div class=\"section-hero\">\n        <div class=\"section-num-big\">12</div>\n        <div class=\"section-tag\">// views, union & indexes</div>\n        <div class=\"section-title\">Views, UNION & Indexes</div>\n        <p class=\"section-desc\">PostgreSQL adds INTERSECT and EXCEPT set operators, Materialized Views for cached results, and a richer set of index types including GIN, GiST, BRIN, and partial indexes.</p>\n      </div>\n\n      <h3 class=\"block-title\">UNION, INTERSECT & EXCEPT</h3>\n      <div class=\"code-block\">\n        <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n        <pre class=\"code\"><span class=\"cm\">-- UNION — combines, removes duplicates (same as MySQL)</span>\n<span class=\"kw\">SELECT</span> name <span class=\"kw\">FROM</span> users\n<span class=\"kw\">UNION</span>\n<span class=\"kw\">SELECT</span> name <span class=\"kw\">FROM</span> admin_users;\n\n<span class=\"cm\">-- INTERSECT — rows present in BOTH (MySQL lacks this)</span>\n<span class=\"kw\">SELECT</span> email <span class=\"kw\">FROM</span> users\n<span class=\"kw\">INTERSECT</span>\n<span class=\"kw\">SELECT</span> email <span class=\"kw\">FROM</span> admin_users;\n\n<span class=\"cm\">-- EXCEPT — rows in first but NOT in second (MySQL lacks this)</span>\n<span class=\"kw\">SELECT</span> email <span class=\"kw\">FROM</span> users\n<span class=\"kw\">EXCEPT</span>\n<span class=\"kw\">SELECT</span> email <span class=\"kw\">FROM</span> admin_users;</pre>\n      </div>\n\n      <h3 class=\"block-title\">Views</h3>\n      <div class=\"code-block\">\n        <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n        <pre class=\"code\"><span class=\"kw\">CREATE</span> <span class=\"kw\">VIEW</span> high_salary_users <span class=\"kw\">AS</span>\n<span class=\"kw\">SELECT</span> id, name, salary <span class=\"kw\">FROM</span> users\n<span class=\"kw\">WHERE</span> salary > <span class=\"num\">70000</span>;\n\n<span class=\"kw\">SELECT</span> * <span class=\"kw\">FROM</span> high_salary_users;\n<span class=\"kw\">DROP</span> <span class=\"kw\">VIEW</span> high_salary_users;</pre>\n      </div>\n\n      <div class=\"adv-box\">\n        <div class=\"adv-header\"><span class=\"adv-icon\">⚡</span><span class=\"adv-label\">Advanced — Materialized Views (PostgreSQL-only)</span><span class=\"adv-badge\">HIGH PRIORITY</span></div>\n        <div class=\"adv-body\">\n          <p class=\"prose\">A regular view runs its query every time. A materialized view stores the result physically — perfect for expensive aggregation queries. You refresh it on demand.</p>\n          <div class=\"code-block\">\n            <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n            <pre class=\"code\"><span class=\"kw\">CREATE MATERIALIZED VIEW</span> monthly_report <span class=\"kw\">AS</span>\n<span class=\"kw\">SELECT</span>\n  <span class=\"fn\">DATE_TRUNC</span>(<span class=\"str\">'month'</span>, created_at) <span class=\"kw\">AS</span> month,\n  gender,\n  <span class=\"fn\">AVG</span>(salary)  <span class=\"kw\">AS</span> avg_salary,\n  <span class=\"fn\">COUNT</span>(*) <span class=\"kw\">AS</span> user_count\n<span class=\"kw\">FROM</span> users <span class=\"kw\">GROUP BY</span> <span class=\"num\">1</span>, <span class=\"num\">2</span>;\n\n<span class=\"cm\">-- Refresh when data changes</span>\n<span class=\"kw\">REFRESH MATERIALIZED VIEW</span> monthly_report;\n\n<span class=\"cm\">-- Refresh without blocking reads (requires unique index on view)</span>\n<span class=\"kw\">REFRESH MATERIALIZED VIEW CONCURRENTLY</span> monthly_report;</pre>\n          </div>\n        </div>\n      </div>\n\n      <h3 class=\"block-title\">Indexes</h3>\n      <div class=\"code-block\">\n        <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n        <pre class=\"code\"><span class=\"kw\">CREATE INDEX</span> idx_email <span class=\"kw\">ON</span> users(email);\n<span class=\"kw\">CREATE INDEX</span> idx_gender_salary <span class=\"kw\">ON</span> users(gender, salary);\n\\d users                  <span class=\"cm\">-- see indexes in psql</span>\n<span class=\"kw\">DROP INDEX</span> idx_email;    <span class=\"cm\">-- no ON table needed (unlike MySQL)</span></pre>\n      </div>\n\n      <div class=\"adv-box\">\n        <div class=\"adv-header\"><span class=\"adv-icon\">⚡</span><span class=\"adv-label\">Advanced — PostgreSQL Index Types</span><span class=\"adv-badge\">INTERVIEW</span></div>\n        <div class=\"adv-body\">\n          <table class=\"data-table\">\n            <tr><th>Type</th><th>Use Case</th><th>Syntax</th></tr>\n            <tr><td><span class=\"badge-tbl b-blue\">B-Tree</span></td><td>Equality and range queries (default)</td><td><code>CREATE INDEX</code></td></tr>\n            <tr><td><span class=\"badge-tbl b-purple\">Hash</span></td><td>Equality only — faster for <code>=</code></td><td><code>USING HASH</code></td></tr>\n            <tr><td><span class=\"badge-tbl b-green\">GIN</span></td><td>JSONB, arrays, full-text search</td><td><code>USING GIN</code></td></tr>\n            <tr><td><span class=\"badge-tbl b-orange\">GiST</span></td><td>Geometric, range types, fuzzy text</td><td><code>USING GIST</code></td></tr>\n            <tr><td><span class=\"badge-tbl b-yellow\">BRIN</span></td><td>Very large sequential tables (IoT)</td><td><code>USING BRIN</code></td></tr>\n          </table>\n          <div class=\"code-block\">\n            <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n            <pre class=\"code\"><span class=\"cm\">-- Partial index — only index active users (smaller, faster)</span>\n<span class=\"kw\">CREATE INDEX</span> idx_active_email <span class=\"kw\">ON</span> users(email) <span class=\"kw\">WHERE</span> is_active = <span class=\"kw\">TRUE</span>;\n\n<span class=\"cm\">-- Expression index — index on computed value</span>\n<span class=\"kw\">CREATE INDEX</span> idx_lower_email <span class=\"kw\">ON</span> users(<span class=\"fn\">LOWER</span>(email));\n\n<span class=\"cm\">-- CONCURRENTLY — no table lock during index build</span>\n<span class=\"kw\">CREATE INDEX CONCURRENTLY</span> idx_salary <span class=\"kw\">ON</span> users(salary);</pre>\n          </div>\n        </div>\n      </div>";

export default function Section11() {
  return <SectionMarkup html={html} />;
}
