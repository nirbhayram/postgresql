import { SectionMarkup } from "./section-markup";

const html = "<div class=\"section-hero\">\n        <div class=\"section-num-big\">11</div>\n        <div class=\"section-tag\">// joining tables</div>\n        <div class=\"section-title\">JOINs</div>\n        <p class=\"section-desc\">All standard MySQL JOINs work in PostgreSQL. PostgreSQL additionally supports FULL OUTER JOIN and the powerful LATERAL JOIN — neither of which MySQL has natively.</p>\n      </div>\n\n      <h3 class=\"block-title\">Standard JOINs</h3>\n      <div class=\"code-block\">\n        <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n        <pre class=\"code\"><span class=\"cm\">-- INNER JOIN — only matching rows</span>\n<span class=\"kw\">SELECT</span> u.name, a.city\n<span class=\"kw\">FROM</span> users u\n<span class=\"kw\">INNER JOIN</span> addresses a <span class=\"kw\">ON</span> u.id = a.user_id;\n\n<span class=\"cm\">-- LEFT JOIN — all users, matched addresses or NULL</span>\n<span class=\"kw\">SELECT</span> u.name, a.city\n<span class=\"kw\">FROM</span> users u\n<span class=\"kw\">LEFT JOIN</span> addresses a <span class=\"kw\">ON</span> u.id = a.user_id;\n\n<span class=\"cm\">-- RIGHT JOIN — all addresses, matched users or NULL</span>\n<span class=\"kw\">SELECT</span> u.name, a.city\n<span class=\"kw\">FROM</span> users u\n<span class=\"kw\">RIGHT JOIN</span> addresses a <span class=\"kw\">ON</span> u.id = a.user_id;\n\n<span class=\"cm\">-- FULL OUTER JOIN — all rows from both tables (MySQL lacks this)</span>\n<span class=\"kw\">SELECT</span> u.name, a.city\n<span class=\"kw\">FROM</span> users u\n<span class=\"kw\">FULL OUTER JOIN</span> addresses a <span class=\"kw\">ON</span> u.id = a.user_id;</pre>\n      </div>\n\n      <h3 class=\"block-title\">Self JOIN</h3>\n      <div class=\"code-block\">\n        <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n        <pre class=\"code\"><span class=\"kw\">SELECT</span>\n  a.id,\n  a.name <span class=\"kw\">AS</span> user_name,\n  b.name <span class=\"kw\">AS</span> referred_by\n<span class=\"kw\">FROM</span> users a\n<span class=\"kw\">LEFT JOIN</span> users b <span class=\"kw\">ON</span> a.referred_by_id = b.id;</pre>\n      </div>\n\n      <div class=\"adv-box\">\n        <div class=\"adv-header\"><span class=\"adv-icon\">⚡</span><span class=\"adv-label\">Advanced — LATERAL JOIN (top-N per group)</span><span class=\"adv-badge\">INTERVIEW</span></div>\n        <div class=\"adv-body\">\n          <p class=\"prose\">LATERAL allows the right side of the join to reference columns from the left side. This is not possible in regular joins and makes top-N-per-group queries elegant.</p>\n          <div class=\"code-block\">\n            <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n            <pre class=\"code\"><span class=\"cm\">-- Get top 3 orders per user</span>\n<span class=\"kw\">SELECT</span> u.name, top_orders.amount, top_orders.order_date\n<span class=\"kw\">FROM</span> users u\n<span class=\"kw\">JOIN LATERAL</span> (\n  <span class=\"kw\">SELECT</span> amount, order_date\n  <span class=\"kw\">FROM</span> orders o\n  <span class=\"kw\">WHERE</span> o.user_id = u.id      <span class=\"cm\">-- references the outer table!</span>\n  <span class=\"kw\">ORDER BY</span> amount <span class=\"kw\">DESC</span>\n  <span class=\"kw\">LIMIT</span> <span class=\"num\">3</span>\n) top_orders <span class=\"kw\">ON TRUE</span>;</pre>\n          </div>\n        </div>\n      </div>";

export default function Section10() {
  return <SectionMarkup html={html} />;
}
