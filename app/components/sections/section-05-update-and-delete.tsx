import { SectionMarkup } from "./section-markup";

const html = "<div class=\"section-hero\">\n        <div class=\"section-num-big\">06</div>\n        <div class=\"section-tag\">// data modification</div>\n        <div class=\"section-title\">UPDATE & DELETE</div>\n        <p class=\"section-desc\">PostgreSQL uses FROM instead of JOIN in UPDATE statements, and USING instead of JOIN in DELETE. RETURNING works on both, giving you immediate feedback.</p>\n      </div>\n\n      <h3 class=\"block-title\">UPDATE</h3>\n      <div class=\"code-block\">\n        <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n        <pre class=\"code\"><span class=\"kw\">UPDATE</span> users <span class=\"kw\">SET</span> name = <span class=\"str\">'Alicia'</span> <span class=\"kw\">WHERE</span> id = <span class=\"num\">1</span>;\n\n<span class=\"cm\">-- Multiple columns</span>\n<span class=\"kw\">UPDATE</span> users\n<span class=\"kw\">SET</span> name = <span class=\"str\">'Robert'</span>, email = <span class=\"str\">'robert@example.com'</span>\n<span class=\"kw\">WHERE</span> id = <span class=\"num\">2</span>;\n\n<span class=\"cm\">-- RETURNING — get the modified rows immediately</span>\n<span class=\"kw\">UPDATE</span> users <span class=\"kw\">SET</span> salary = salary + <span class=\"num\">10000</span>\n<span class=\"kw\">WHERE</span> salary < <span class=\"num\">60000</span>\n<span class=\"kw\">RETURNING</span> id, name, salary;</pre>\n      </div>\n\n      <div class=\"adv-box\">\n        <div class=\"adv-header\"><span class=\"adv-icon\">⚡</span><span class=\"adv-label\">Advanced — UPDATE ... FROM (JOIN-style update)</span><span class=\"adv-badge\">SYNTAX DIFF</span></div>\n        <div class=\"adv-body\">\n          <p class=\"prose\">MySQL uses <code>UPDATE t1 JOIN t2</code>. PostgreSQL uses the <code>FROM</code> clause — different syntax, same idea.</p>\n          <div class=\"code-block\">\n            <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n            <pre class=\"code\"><span class=\"kw\">UPDATE</span> users u\n<span class=\"kw\">SET</span> city = a.city\n<span class=\"kw\">FROM</span> addresses a\n<span class=\"kw\">WHERE</span> u.id = a.user_id;</pre>\n          </div>\n        </div>\n      </div>\n\n      <h3 class=\"block-title\">DELETE</h3>\n      <div class=\"code-block\">\n        <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n        <pre class=\"code\"><span class=\"kw\">DELETE</span> <span class=\"kw\">FROM</span> users <span class=\"kw\">WHERE</span> id = <span class=\"num\">3</span>;\n\n<span class=\"cm\">-- DELETE with RETURNING</span>\n<span class=\"kw\">DELETE</span> <span class=\"kw\">FROM</span> users <span class=\"kw\">WHERE</span> salary < <span class=\"num\">50000</span> <span class=\"kw\">RETURNING</span> *;\n\n<span class=\"cm\">-- DELETE with join (PostgreSQL uses USING, not JOIN)</span>\n<span class=\"kw\">DELETE</span> <span class=\"kw\">FROM</span> users u\n<span class=\"kw\">USING</span> addresses a\n<span class=\"kw\">WHERE</span> u.id = a.user_id <span class=\"kw\">AND</span> a.city = <span class=\"str\">'Delhi'</span>;</pre>\n      </div>\n\n      <div class=\"adv-box\">\n        <div class=\"adv-header\"><span class=\"adv-icon\">⚡</span><span class=\"adv-label\">Advanced — TRUNCATE vs DELETE vs DROP</span><span class=\"adv-badge\">INTERVIEW</span></div>\n        <div class=\"adv-body\">\n          <table class=\"data-table\">\n            <tr><th>Command</th><th>Removes</th><th>Rollback?</th><th>Speed</th><th>Triggers?</th></tr>\n            <tr><td><code>DELETE</code></td><td>Rows matching WHERE</td><td>Yes</td><td>Slow (row-by-row)</td><td>Yes</td></tr>\n            <tr><td><code>TRUNCATE</code></td><td>All rows</td><td><span class=\"badge-tbl b-green\">Yes in PG</span></td><td>Fast</td><td>No</td></tr>\n            <tr><td><code>DROP TABLE</code></td><td>Table + all data</td><td>Yes</td><td>Immediate</td><td>No</td></tr>\n          </table>\n          <div class=\"info-tip info-box\" style=\"margin:0\"><span class=\"icon\">💡</span><span>Unlike MySQL, PostgreSQL's <code>TRUNCATE</code> is fully transactional — you CAN roll it back inside a BEGIN/ROLLBACK block.</span></div>\n        </div>\n      </div>";

export default function Section5() {
  return <SectionMarkup html={html} />;
}
