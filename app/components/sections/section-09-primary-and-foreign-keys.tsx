import { SectionMarkup } from "./section-markup";

const html = "<div class=\"section-hero\">\n        <div class=\"section-num-big\">10</div>\n        <div class=\"section-tag\">// relationships</div>\n        <div class=\"section-title\">Primary Keys & Foreign Keys</div>\n        <p class=\"section-desc\">Same concepts as MySQL — different syntax. PostgreSQL uses ALTER SEQUENCE instead of AUTO_INCREMENT, and DROP CONSTRAINT instead of DROP FOREIGN KEY.</p>\n      </div>\n\n      <h3 class=\"block-title\">Primary Keys</h3>\n      <div class=\"code-block\">\n        <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n        <pre class=\"code\"><span class=\"kw\">CREATE</span> <span class=\"kw\">TABLE</span> users (\n  id   <span class=\"fn\">SERIAL</span> <span class=\"kw\">PRIMARY KEY</span>,\n  name <span class=\"fn\">VARCHAR</span>(<span class=\"num\">100</span>)\n);\n\n<span class=\"cm\">-- Change starting value (replaces ALTER TABLE AUTO_INCREMENT)</span>\n<span class=\"kw\">ALTER SEQUENCE</span> users_id_seq <span class=\"kw\">RESTART WITH</span> <span class=\"num\">1000</span>;\n\n<span class=\"cm\">-- Add PK later</span>\n<span class=\"kw\">ALTER</span> <span class=\"kw\">TABLE</span> users <span class=\"kw\">ADD</span> <span class=\"kw\">PRIMARY KEY</span> (id);\n\n<span class=\"cm\">-- Drop PK</span>\n<span class=\"kw\">ALTER</span> <span class=\"kw\">TABLE</span> users <span class=\"kw\">DROP CONSTRAINT</span> users_pkey;</pre>\n      </div>\n\n      <h3 class=\"block-title\">Foreign Keys</h3>\n      <div class=\"code-block\">\n        <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n        <pre class=\"code\"><span class=\"kw\">CREATE</span> <span class=\"kw\">TABLE</span> addresses (\n  id      <span class=\"fn\">SERIAL</span> <span class=\"kw\">PRIMARY KEY</span>,\n  user_id <span class=\"fn\">INT</span> <span class=\"kw\">REFERENCES</span> users(id) <span class=\"kw\">ON DELETE CASCADE</span>,\n  street  <span class=\"fn\">VARCHAR</span>(<span class=\"num\">255</span>),\n  city    <span class=\"fn\">VARCHAR</span>(<span class=\"num\">100</span>)\n);\n\n<span class=\"cm\">-- Add FK with name (via ALTER)</span>\n<span class=\"kw\">ALTER</span> <span class=\"kw\">TABLE</span> addresses\n  <span class=\"kw\">ADD CONSTRAINT</span> fk_user\n  <span class=\"kw\">FOREIGN KEY</span> (user_id) <span class=\"kw\">REFERENCES</span> users(id)\n  <span class=\"kw\">ON DELETE CASCADE</span>;\n\n<span class=\"cm\">-- Drop FK — use DROP CONSTRAINT (not DROP FOREIGN KEY like MySQL)</span>\n<span class=\"kw\">ALTER</span> <span class=\"kw\">TABLE</span> addresses <span class=\"kw\">DROP CONSTRAINT</span> fk_user;</pre>\n      </div>\n\n      <h3 class=\"block-title\">ON DELETE Options</h3>\n      <table class=\"data-table\">\n        <tr><th>Option</th><th>Behaviour on parent delete</th></tr>\n        <tr><td><code>CASCADE</code></td><td>Deletes child rows automatically</td></tr>\n        <tr><td><code>SET NULL</code></td><td>Sets FK column to NULL</td></tr>\n        <tr><td><code>SET DEFAULT</code></td><td>Sets FK to its default value</td></tr>\n        <tr><td><code>RESTRICT</code></td><td>Prevents deletion if children exist (checked immediately)</td></tr>\n        <tr><td><code>NO ACTION</code></td><td>Like RESTRICT but checked at end of transaction (default)</td></tr>\n      </table>\n\n      <div class=\"adv-box\">\n        <div class=\"adv-header\"><span class=\"adv-icon\">⚡</span><span class=\"adv-label\">Advanced — Composite Primary Keys (many-to-many)</span><span class=\"adv-badge\">PATTERN</span></div>\n        <div class=\"adv-body\">\n          <div class=\"code-block\">\n            <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n            <pre class=\"code\"><span class=\"kw\">CREATE</span> <span class=\"kw\">TABLE</span> user_roles (\n  user_id <span class=\"fn\">INT</span> <span class=\"kw\">REFERENCES</span> users(id) <span class=\"kw\">ON DELETE CASCADE</span>,\n  role_id <span class=\"fn\">INT</span> <span class=\"kw\">REFERENCES</span> roles(id) <span class=\"kw\">ON DELETE CASCADE</span>,\n  granted_at <span class=\"fn\">TIMESTAMPTZ</span> <span class=\"kw\">DEFAULT</span> <span class=\"fn\">NOW</span>(),\n  <span class=\"kw\">PRIMARY KEY</span> (user_id, role_id)\n);</pre>\n          </div>\n        </div>\n      </div>";

export default function Section9() {
  return <SectionMarkup html={html} />;
}
