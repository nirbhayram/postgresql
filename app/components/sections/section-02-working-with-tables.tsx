import { SectionMarkup } from "./section-markup";

const html = "<div class=\"section-hero\">\n        <div class=\"section-num-big\">03</div>\n        <div class=\"section-tag\">// table management</div>\n        <div class=\"section-title\">Working with Tables</div>\n        <p class=\"section-desc\">ALTER TABLE in PostgreSQL uses a different syntax from MySQL, especially for column modifications. The USING clause for type changes is a PostgreSQL-specific requirement.</p>\n      </div>\n\n      <h3 class=\"block-title\">Selecting & Renaming</h3>\n      <div class=\"code-block\">\n        <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n        <pre class=\"code\"><span class=\"kw\">SELECT</span> * <span class=\"kw\">FROM</span> users;\n<span class=\"kw\">SELECT</span> name, email <span class=\"kw\">FROM</span> users;\n\n<span class=\"cm\">-- Rename table (same as MySQL)</span>\n<span class=\"kw\">ALTER</span> <span class=\"kw\">TABLE</span> users <span class=\"kw\">RENAME</span> <span class=\"kw\">TO</span> customers;</pre>\n      </div>\n\n      <h3 class=\"block-title\">Altering Columns</h3>\n      <div class=\"code-block\">\n        <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n        <pre class=\"code\"><span class=\"cm\">-- Add a column</span>\n<span class=\"kw\">ALTER</span> <span class=\"kw\">TABLE</span> users <span class=\"kw\">ADD</span> <span class=\"kw\">COLUMN</span> is_active <span class=\"fn\">BOOLEAN</span> <span class=\"kw\">DEFAULT</span> <span class=\"kw\">TRUE</span>;\n\n<span class=\"cm\">-- Drop a column</span>\n<span class=\"kw\">ALTER</span> <span class=\"kw\">TABLE</span> users <span class=\"kw\">DROP</span> <span class=\"kw\">COLUMN</span> is_active;\n\n<span class=\"cm\">-- Change column type (USING casts existing data)</span>\n<span class=\"kw\">ALTER</span> <span class=\"kw\">TABLE</span> users <span class=\"kw\">ALTER</span> <span class=\"kw\">COLUMN</span> name <span class=\"kw\">TYPE</span> <span class=\"fn\">VARCHAR</span>(<span class=\"num\">150</span>);\n\n<span class=\"cm\">-- Rename a column (PostgreSQL uses RENAME COLUMN, not CHANGE)</span>\n<span class=\"kw\">ALTER</span> <span class=\"kw\">TABLE</span> users <span class=\"kw\">RENAME</span> <span class=\"kw\">COLUMN</span> city <span class=\"kw\">TO</span> location;\n\n<span class=\"cm\">-- Set / Drop column default</span>\n<span class=\"kw\">ALTER</span> <span class=\"kw\">TABLE</span> users <span class=\"kw\">ALTER</span> <span class=\"kw\">COLUMN</span> is_active <span class=\"kw\">SET DEFAULT</span> <span class=\"kw\">FALSE</span>;\n<span class=\"kw\">ALTER</span> <span class=\"kw\">TABLE</span> users <span class=\"kw\">ALTER</span> <span class=\"kw\">COLUMN</span> is_active <span class=\"kw\">DROP DEFAULT</span>;</pre>\n      </div>\n\n      <div class=\"info-warn info-box\"><span class=\"icon\">⚠️</span><span>PostgreSQL does not support <code>FIRST</code> or <code>AFTER</code> for moving columns — column order cannot be changed after creation without recreating the table. This is rarely needed since SELECT can reorder columns in output.</span></div>\n\n      <div class=\"adv-box\">\n        <div class=\"adv-header\"><span class=\"adv-icon\">⚡</span><span class=\"adv-label\">Advanced — USING for incompatible type changes</span><span class=\"adv-badge\">GOTCHA</span></div>\n        <div class=\"adv-body\">\n          <p class=\"prose\">When converting a column to an incompatible type, PostgreSQL requires a USING clause to specify how to cast the old data. The <code>::</code> operator is PostgreSQL's shorthand for <code>CAST(... AS ...)</code>.</p>\n          <div class=\"code-block\">\n            <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n            <pre class=\"code\"><span class=\"cm\">-- Convert text column to integer</span>\n<span class=\"kw\">ALTER</span> <span class=\"kw\">TABLE</span> users\n  <span class=\"kw\">ALTER</span> <span class=\"kw\">COLUMN</span> salary <span class=\"kw\">TYPE</span> <span class=\"fn\">BIGINT</span>\n  <span class=\"kw\">USING</span> salary::<span class=\"fn\">BIGINT</span>;\n\n<span class=\"cm\">-- The :: operator is PostgreSQL-specific cast syntax</span>\n<span class=\"cm\">-- Equivalent: CAST(salary AS BIGINT)</span></pre>\n          </div>\n        </div>\n      </div>\n\n      <h3 class=\"block-title\">Inspecting Table Structure</h3>\n      <div class=\"code-block\">\n        <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n        <pre class=\"code\">\\d users          <span class=\"cm\">-- psql CLI — shows columns, indexes, constraints</span>\n\\d+ users         <span class=\"cm\">-- extended info including storage and comments</span>\n\n<span class=\"cm\">-- Works in any client (equivalent to MySQL's DESCRIBE)</span>\n<span class=\"kw\">SELECT</span> column_name, data_type, is_nullable, column_default\n<span class=\"kw\">FROM</span> information_schema.columns\n<span class=\"kw\">WHERE</span> table_name = <span class=\"str\">'users'</span> <span class=\"kw\">AND</span> table_schema = <span class=\"str\">'public'</span>;</pre>\n      </div>";

export default function Section2() {
  return <SectionMarkup html={html} />;
}
