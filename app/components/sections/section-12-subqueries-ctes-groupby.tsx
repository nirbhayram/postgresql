import { SectionMarkup } from "./section-markup";

const html = "<div class=\"section-hero\">\n        <div class=\"section-num-big\">13</div>\n        <div class=\"section-tag\">// advanced querying</div>\n        <div class=\"section-title\">Subqueries, CTEs & GROUP BY</div>\n        <p class=\"section-desc\">PostgreSQL's CTEs (WITH clauses) and recursive CTEs are far more powerful than nested subqueries. Recursive CTEs can traverse tree structures in a single query.</p>\n      </div>\n\n      <h3 class=\"block-title\">Subqueries</h3>\n      <div class=\"code-block\">\n        <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n        <pre class=\"code\"><span class=\"cm\">-- Scalar subquery</span>\n<span class=\"kw\">SELECT</span> id, name, salary <span class=\"kw\">FROM</span> users\n<span class=\"kw\">WHERE</span> salary > (<span class=\"kw\">SELECT</span> <span class=\"fn\">AVG</span>(salary) <span class=\"kw\">FROM</span> users);\n\n<span class=\"cm\">-- Subquery with IN</span>\n<span class=\"kw\">SELECT</span> id, name <span class=\"kw\">FROM</span> users\n<span class=\"kw\">WHERE</span> referred_by_id <span class=\"kw\">IN</span> (\n  <span class=\"kw\">SELECT</span> id <span class=\"kw\">FROM</span> users <span class=\"kw\">WHERE</span> salary > <span class=\"num\">75000</span>\n);</pre>\n      </div>\n\n      <div class=\"adv-box\">\n        <div class=\"adv-header\"><span class=\"adv-icon\">⚡</span><span class=\"adv-label\">Advanced — CTEs (Common Table Expressions)</span><span class=\"adv-badge\">BEST PRACTICE</span></div>\n        <div class=\"adv-body\">\n          <div class=\"code-block\">\n            <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n            <pre class=\"code\"><span class=\"kw\">WITH</span> high_earners <span class=\"kw\">AS</span> (\n  <span class=\"kw\">SELECT</span> id, name, salary <span class=\"kw\">FROM</span> users\n  <span class=\"kw\">WHERE</span> salary > <span class=\"num\">75000</span>\n),\ntheir_cities <span class=\"kw\">AS</span> (\n  <span class=\"kw\">SELECT</span> a.city, a.user_id\n  <span class=\"kw\">FROM</span> addresses a\n  <span class=\"kw\">JOIN</span> high_earners h <span class=\"kw\">ON</span> a.user_id = h.id\n)\n<span class=\"kw\">SELECT</span> h.name, h.salary, tc.city\n<span class=\"kw\">FROM</span> high_earners h\n<span class=\"kw\">JOIN</span> their_cities tc <span class=\"kw\">ON</span> h.id = tc.user_id;</pre>\n          </div>\n        </div>\n      </div>\n\n      <div class=\"adv-box\">\n        <div class=\"adv-header\"><span class=\"adv-icon\">⚡</span><span class=\"adv-label\">Advanced — Recursive CTEs (tree/hierarchy traversal)</span><span class=\"adv-badge\">INTERVIEW</span></div>\n        <div class=\"adv-body\">\n          <p class=\"prose\">Recursive CTEs solve hierarchical data problems — like finding the full referral chain — without stored procedures or multiple round-trips.</p>\n          <div class=\"code-block\">\n            <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n            <pre class=\"code\"><span class=\"kw\">WITH RECURSIVE</span> referral_chain <span class=\"kw\">AS</span> (\n  <span class=\"cm\">-- Base case: start with user 1</span>\n  <span class=\"kw\">SELECT</span> id, name, referred_by_id, <span class=\"num\">0</span> <span class=\"kw\">AS</span> depth\n  <span class=\"kw\">FROM</span> users <span class=\"kw\">WHERE</span> id = <span class=\"num\">1</span>\n\n  <span class=\"kw\">UNION ALL</span>\n\n  <span class=\"cm\">-- Recursive case: find users referred by current level</span>\n  <span class=\"kw\">SELECT</span> u.id, u.name, u.referred_by_id, rc.depth + <span class=\"num\">1</span>\n  <span class=\"kw\">FROM</span> users u\n  <span class=\"kw\">JOIN</span> referral_chain rc <span class=\"kw\">ON</span> u.referred_by_id = rc.id\n)\n<span class=\"kw\">SELECT</span> * <span class=\"kw\">FROM</span> referral_chain <span class=\"kw\">ORDER BY</span> depth;</pre>\n          </div>\n        </div>\n      </div>\n\n      <h3 class=\"block-title\">GROUP BY and HAVING</h3>\n      <div class=\"code-block\">\n        <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n        <pre class=\"code\"><span class=\"kw\">SELECT</span> gender, <span class=\"fn\">AVG</span>(salary) <span class=\"kw\">AS</span> avg_salary\n<span class=\"kw\">FROM</span> users\n<span class=\"kw\">GROUP BY</span> gender\n<span class=\"kw\">HAVING</span> <span class=\"fn\">AVG</span>(salary) > <span class=\"num\">75000</span>;\n\n<span class=\"cm\">-- ROLLUP for subtotals (same as MySQL)</span>\n<span class=\"kw\">SELECT</span> gender, <span class=\"fn\">COUNT</span>(*) <span class=\"kw\">FROM</span> users\n<span class=\"kw\">GROUP BY</span> <span class=\"kw\">ROLLUP</span>(gender);</pre>\n      </div>\n\n      <div class=\"adv-box\">\n        <div class=\"adv-header\"><span class=\"adv-icon\">⚡</span><span class=\"adv-label\">Advanced — GROUPING SETS & CUBE</span><span class=\"adv-badge\">ANALYTICS</span></div>\n        <div class=\"adv-body\">\n          <div class=\"code-block\">\n            <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n            <pre class=\"code\"><span class=\"cm\">-- GROUPING SETS — multiple groupings in one query</span>\n<span class=\"kw\">SELECT</span> gender, <span class=\"fn\">DATE_TRUNC</span>(<span class=\"str\">'year'</span>, created_at) <span class=\"kw\">AS</span> yr, <span class=\"fn\">COUNT</span>(*)\n<span class=\"kw\">FROM</span> users\n<span class=\"kw\">GROUP BY GROUPING SETS</span> (\n  (gender),       <span class=\"cm\">-- group by gender only</span>\n  (yr),           <span class=\"cm\">-- group by year only</span>\n  (gender, yr),   <span class=\"cm\">-- group by both</span>\n  ()              <span class=\"cm\">-- grand total</span>\n);\n\n<span class=\"cm\">-- CUBE — all possible combinations automatically</span>\n<span class=\"kw\">SELECT</span> gender, city, <span class=\"fn\">AVG</span>(salary)\n<span class=\"kw\">FROM</span> users\n<span class=\"kw\">GROUP BY</span> <span class=\"kw\">CUBE</span>(gender, city);</pre>\n          </div>\n        </div>\n      </div>";

export default function Section12() {
  return <SectionMarkup html={html} />;
}
