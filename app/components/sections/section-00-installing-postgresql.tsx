import { SectionMarkup } from "./section-markup";

const html = "<div class=\"section-hero\">\n        <div class=\"section-num-big\">01</div>\n        <div class=\"section-tag\">// getting started</div>\n        <div class=\"section-title\">Installing PostgreSQL</div>\n        <p class=\"section-desc\">Set up PostgreSQL on any platform. Unlike MySQL's storage-engine architecture, PostgreSQL is a unified engine prioritising correctness, standards-compliance, and extensibility.</p>\n      </div>\n\n      <h3 class=\"block-title\">What is PostgreSQL?</h3>\n      <p class=\"prose\">PostgreSQL (also called \"Postgres\") is a powerful, open-source object-relational database with 35+ years of development. It is ACID-compliant by default, supports advanced data types (JSON, Arrays, UUID, custom types), and is extensible via extensions like <code>pg_trgm</code>, <code>PostGIS</code>, and <code>pgvector</code>.</p>\n\n      <h3 class=\"block-title\">Windows / macOS Installation</h3>\n      <p class=\"prose\">Download from <code>https://www.postgresql.org/download/</code>. Choose version 16 or 17. The installer bundles pgAdmin (GUI), psql (CLI), and the server. You'll set a password for the <code>postgres</code> superuser during setup.</p>\n\n      <h3 class=\"block-title\">Linux (Ubuntu) Installation</h3>\n      <div class=\"code-block\">\n        <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">BASH</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n        <pre class=\"code\"><span class=\"cm\"># Add PostgreSQL apt repository and install</span>\nsudo apt install -y postgresql postgresql-contrib\n\n<span class=\"cm\"># Start and enable the service</span>\nsudo systemctl start postgresql\nsudo systemctl enable postgresql\n\n<span class=\"cm\"># Log in as the postgres system user</span>\nsudo -i -u postgres psql\n\n<span class=\"cm\"># Inside psql — create a role and database</span>\n<span class=\"kw\">CREATE</span> <span class=\"kw\">USER</span> harry <span class=\"kw\">WITH</span> PASSWORD <span class=\"str\">'securepassword'</span>;\n<span class=\"kw\">CREATE</span> <span class=\"kw\">DATABASE</span> mydb <span class=\"kw\">OWNER</span> harry;\n<span class=\"kw\">GRANT</span> <span class=\"kw\">ALL</span> <span class=\"kw\">PRIVILEGES</span> <span class=\"kw\">ON</span> <span class=\"kw\">DATABASE</span> mydb <span class=\"kw\">TO</span> harry;\n\\q\n\n<span class=\"cm\"># Test login</span>\npsql -U harry -d mydb -h localhost</pre>\n      </div>\n\n      <h3 class=\"block-title\">MySQL vs PostgreSQL — Key Differences</h3>\n      <div class=\"diff-grid\">\n        <div class=\"diff-card diff-mysql\">\n          <div class=\"diff-card-head\">⚡ MySQL</div>\n          <pre class=\"code\">sudo mysql\n<span class=\"kw\">CREATE</span> <span class=\"kw\">USER</span> <span class=\"str\">'harry'</span>@<span class=\"str\">'localhost'</span>\n  IDENTIFIED BY <span class=\"str\">'password'</span>;\n<span class=\"kw\">GRANT</span> <span class=\"kw\">ALL</span> <span class=\"kw\">PRIVILEGES</span> <span class=\"kw\">ON</span> *.*\n  <span class=\"kw\">TO</span> <span class=\"str\">'harry'</span>@<span class=\"str\">'localhost'</span>;\n<span class=\"kw\">FLUSH</span> PRIVILEGES;  <span class=\"cm\">-- required!</span></pre>\n        </div>\n        <div class=\"diff-card diff-pg\">\n          <div class=\"diff-card-head\">🐘 PostgreSQL</div>\n          <pre class=\"code\">sudo -i -u postgres psql\n<span class=\"kw\">CREATE</span> <span class=\"kw\">USER</span> harry\n  <span class=\"kw\">WITH</span> PASSWORD <span class=\"str\">'password'</span>;\n<span class=\"kw\">GRANT</span> <span class=\"kw\">ALL</span> <span class=\"kw\">PRIVILEGES</span>\n  <span class=\"kw\">ON</span> <span class=\"kw\">DATABASE</span> mydb <span class=\"kw\">TO</span> harry;\n<span class=\"cm\">-- No FLUSH needed</span></pre>\n        </div>\n      </div>\n\n      <div class=\"info-tip info-box\"><span class=\"icon\">💡</span><span>Key difference: MySQL requires <code>FLUSH PRIVILEGES</code> after grants. PostgreSQL changes take effect immediately — no flush needed.</span></div>\n\n      <div class=\"adv-box\">\n        <div class=\"adv-header\"><span class=\"adv-icon\">⚡</span><span class=\"adv-label\">Advanced — Roles vs Users (RBAC)</span><span class=\"adv-badge\">INTERVIEW</span></div>\n        <div class=\"adv-body\">\n          <p class=\"prose\">In PostgreSQL, <code>USER</code> and <code>ROLE</code> are identical — a USER is simply a ROLE with the LOGIN privilege. You can create a role without login to act as a permission group, then grant it to other roles.</p>\n          <div class=\"code-block\">\n            <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n            <pre class=\"code\"><span class=\"kw\">CREATE</span> <span class=\"kw\">ROLE</span> read_only;  <span class=\"cm\">-- no login privilege</span>\n<span class=\"kw\">GRANT</span> <span class=\"kw\">SELECT</span> <span class=\"kw\">ON</span> <span class=\"kw\">ALL</span> <span class=\"kw\">TABLES</span> <span class=\"kw\">IN</span> <span class=\"kw\">SCHEMA</span> public <span class=\"kw\">TO</span> read_only;\n<span class=\"kw\">GRANT</span> read_only <span class=\"kw\">TO</span> harry;  <span class=\"cm\">-- harry inherits SELECT on all tables</span></pre>\n          </div>\n        </div>\n      </div>";

export default function Section0() {
  return <SectionMarkup html={html} />;
}
