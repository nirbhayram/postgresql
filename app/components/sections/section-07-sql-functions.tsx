import { SectionMarkup } from "./section-markup";

const html = "<div class=\"section-hero\">\n        <div class=\"section-num-big\">08</div>\n        <div class=\"section-tag\">// built-in functions</div>\n        <div class=\"section-title\">SQL Functions</div>\n        <p class=\"section-desc\">Most aggregate and string functions are identical between MySQL and PostgreSQL. The key differences are in date functions — PostgreSQL uses EXTRACT(), AGE(), and DATE_TRUNC().</p>\n      </div>\n\n      <h3 class=\"block-title\">Aggregate Functions</h3>\n      <div class=\"code-block\">\n        <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n        <pre class=\"code\"><span class=\"kw\">SELECT</span> <span class=\"fn\">COUNT</span>(*) <span class=\"kw\">FROM</span> users;\n<span class=\"kw\">SELECT</span> <span class=\"fn\">COUNT</span>(*) <span class=\"kw\">FROM</span> users <span class=\"kw\">WHERE</span> gender = <span class=\"str\">'Female'</span>;\n<span class=\"kw\">SELECT</span> <span class=\"fn\">MIN</span>(salary), <span class=\"fn\">MAX</span>(salary) <span class=\"kw\">FROM</span> users;\n<span class=\"kw\">SELECT</span> <span class=\"fn\">SUM</span>(salary) <span class=\"kw\">AS</span> total_payroll <span class=\"kw\">FROM</span> users;\n<span class=\"kw\">SELECT</span> <span class=\"fn\">AVG</span>(salary) <span class=\"kw\">AS</span> avg_salary <span class=\"kw\">FROM</span> users;\n<span class=\"kw\">SELECT</span> gender, <span class=\"fn\">AVG</span>(salary) <span class=\"kw\">FROM</span> users <span class=\"kw\">GROUP BY</span> gender;</pre>\n      </div>\n\n      <h3 class=\"block-title\">String Functions</h3>\n      <div class=\"code-block\">\n        <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n        <pre class=\"code\"><span class=\"kw\">SELECT</span> name, <span class=\"fn\">LENGTH</span>(name) <span class=\"kw\">FROM</span> users;\n<span class=\"kw\">SELECT</span> <span class=\"fn\">LOWER</span>(name), <span class=\"fn\">UPPER</span>(name) <span class=\"kw\">FROM</span> users;\n\n<span class=\"cm\">-- || is PostgreSQL's string concatenation operator</span>\n<span class=\"kw\">SELECT</span> name || <span class=\"str\">' <'</span> || email || <span class=\"str\">'>'</span> <span class=\"kw\">AS</span> user_contact <span class=\"kw\">FROM</span> users;\n\n<span class=\"cm\">-- SPLIT_PART — split on delimiter (no MySQL equivalent)</span>\n<span class=\"kw\">SELECT</span> <span class=\"fn\">SPLIT_PART</span>(email, <span class=\"str\">'@'</span>, <span class=\"num\">2</span>) <span class=\"kw\">AS</span> domain <span class=\"kw\">FROM</span> users;\n\n<span class=\"cm\">-- SUBSTRING</span>\n<span class=\"kw\">SELECT</span> <span class=\"fn\">SUBSTRING</span>(name <span class=\"kw\">FROM</span> <span class=\"num\">1</span> <span class=\"kw\">FOR</span> <span class=\"num\">3</span>) <span class=\"kw\">FROM</span> users;\n\n<span class=\"cm\">-- TRIM</span>\n<span class=\"kw\">SELECT</span> <span class=\"fn\">TRIM</span>(<span class=\"str\">'  Alice  '</span>);</pre>\n      </div>\n\n      <h3 class=\"block-title\">Date Functions — Different from MySQL</h3>\n      <div class=\"code-block\">\n        <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n        <pre class=\"code\"><span class=\"cm\">-- Current timestamps</span>\n<span class=\"kw\">SELECT</span> <span class=\"fn\">NOW</span>();           <span class=\"cm\">-- timestamp with timezone</span>\n<span class=\"kw\">SELECT</span> <span class=\"fn\">CURRENT_DATE</span>;   <span class=\"cm\">-- date only</span>\n<span class=\"kw\">SELECT</span> <span class=\"fn\">CURRENT_TIME</span>;   <span class=\"cm\">-- time only</span>\n\n<span class=\"cm\">-- EXTRACT replaces YEAR(), MONTH(), DAY()</span>\n<span class=\"kw\">SELECT</span> <span class=\"fn\">EXTRACT</span>(<span class=\"kw\">YEAR FROM</span> date_of_birth) <span class=\"kw\">AS</span> birth_year <span class=\"kw\">FROM</span> users;\n<span class=\"kw\">SELECT</span> <span class=\"fn\">EXTRACT</span>(<span class=\"kw\">DOW FROM</span> created_at) <span class=\"kw\">AS</span> day_of_week <span class=\"kw\">FROM</span> users;\n\n<span class=\"cm\">-- AGE replaces TIMESTAMPDIFF</span>\n<span class=\"kw\">SELECT</span> name, <span class=\"fn\">AGE</span>(date_of_birth) <span class=\"kw\">AS</span> age <span class=\"kw\">FROM</span> users;\n<span class=\"kw\">SELECT</span> <span class=\"fn\">EXTRACT</span>(<span class=\"kw\">YEAR FROM</span> <span class=\"fn\">AGE</span>(date_of_birth)) <span class=\"kw\">AS</span> years_old <span class=\"kw\">FROM</span> users;\n\n<span class=\"cm\">-- INTERVAL arithmetic</span>\n<span class=\"kw\">SELECT</span> * <span class=\"kw\">FROM</span> users <span class=\"kw\">WHERE</span> created_at > <span class=\"fn\">NOW</span>() - <span class=\"kw\">INTERVAL</span> <span class=\"str\">'30 days'</span>;\n<span class=\"kw\">SELECT</span> <span class=\"fn\">NOW</span>() + <span class=\"kw\">INTERVAL</span> <span class=\"str\">'1 year 2 months 15 days'</span>;</pre>\n      </div>\n\n      <div class=\"adv-box\">\n        <div class=\"adv-header\"><span class=\"adv-icon\">⚡</span><span class=\"adv-label\">Advanced — DATE_TRUNC, COALESCE, NULLIF, CASE WHEN</span><span class=\"adv-badge\">ANALYTICS</span></div>\n        <div class=\"adv-body\">\n          <div class=\"code-block\">\n            <div class=\"code-bar\"><div class=\"code-dots\"><div class=\"code-dot cd1\"></div><div class=\"code-dot cd2\"></div><div class=\"code-dot cd3\"></div></div><span class=\"code-lang\">SQL</span><button class=\"code-copy\" onclick=\"copyCode(this)\">copy</button></div>\n            <pre class=\"code\"><span class=\"cm\">-- DATE_TRUNC — bucket timestamps (ideal for monthly reports)</span>\n<span class=\"kw\">SELECT</span> <span class=\"fn\">DATE_TRUNC</span>(<span class=\"str\">'month'</span>, created_at) <span class=\"kw\">AS</span> month,\n       <span class=\"fn\">COUNT</span>(*) <span class=\"kw\">AS</span> signups\n<span class=\"kw\">FROM</span> users\n<span class=\"kw\">GROUP BY</span> <span class=\"fn\">DATE_TRUNC</span>(<span class=\"str\">'month'</span>, created_at)\n<span class=\"kw\">ORDER BY</span> <span class=\"num\">1</span>;\n\n<span class=\"cm\">-- COALESCE — returns first non-null value</span>\n<span class=\"kw\">SELECT</span> <span class=\"fn\">COALESCE</span>(salary, <span class=\"num\">0</span>) <span class=\"kw\">FROM</span> users;\n\n<span class=\"cm\">-- NULLIF — returns null if two values are equal (avoid division by zero)</span>\n<span class=\"kw\">SELECT</span> total / <span class=\"fn\">NULLIF</span>(count_value, <span class=\"num\">0</span>) <span class=\"kw\">FROM</span> stats;\n\n<span class=\"cm\">-- CASE WHEN (same as MySQL)</span>\n<span class=\"kw\">SELECT</span> name,\n  <span class=\"kw\">CASE</span>\n    <span class=\"kw\">WHEN</span> salary > <span class=\"num\">80000</span> <span class=\"kw\">THEN</span> <span class=\"str\">'High'</span>\n    <span class=\"kw\">WHEN</span> salary > <span class=\"num\">60000</span> <span class=\"kw\">THEN</span> <span class=\"str\">'Mid'</span>\n    <span class=\"kw\">ELSE</span> <span class=\"str\">'Low'</span>\n  <span class=\"kw\">END</span> <span class=\"kw\">AS</span> salary_band\n<span class=\"kw\">FROM</span> users;</pre>\n          </div>\n        </div>\n      </div>";

export default function Section7() {
  return <SectionMarkup html={html} />;
}
